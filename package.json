{
  "name": "chatgpt-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "yarn && cd client && yarn build && cp -r build/* ../public",
    "start": "nodemon index.js"
  },
  "author": "",
  "type": "module",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "langchain": "^0.0.49",
    "openai": "^3.2.1"
  }
}
